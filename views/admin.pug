extends public
block append style
  each css in styleList
    link(rel='stylesheet', href=css)

block append main
  div#root

block append script
  script.
    var ADMIN_PATH = '#{ADMIN_PATH}'
  each js in scriptList
    script(src=js)
